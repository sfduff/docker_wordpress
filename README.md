# docker_wordpress

```docker-compose up -d```

..to build and run the containers

Open a browser to http://localhost:8000 and..

- Select uk
- Give the site a name
- Add a username
- Clear the password, its important that its empty
- Add you email address
- Discourage search engines from indexing the site

Proceed to the next screen where you’ll be given an automatically generated password.  Use it to login to the admin panel then change it immediately.

